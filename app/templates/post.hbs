{{#link-to 'posts'}}&larr; Back{{/link-to}}
<h1>{{ model.title }}</h1>

<code>{{ model.body }}</code>

{{#each model.comments as |comment|}}
<p>{{comment.body}}</p>
{{/each}}

Title: {{ input value=model.title }}<br>
Body: {{ input value=model.body }}<br>

<button {{action "save"}}>Save</button><br>

<button {{action "generate" model}}>Generate comment</button><br>

<button {{action "paginate" 1}}>Paginate</button>